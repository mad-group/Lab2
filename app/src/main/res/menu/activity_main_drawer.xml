<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
            <item
                android:id="@+id/join_group"
                android:icon="@drawable/ic_add_user"
                android:title="@string/title_activity_join_group" />
            <item
                android:id="@+id/createGroup"
                android:icon="@drawable/ic_new_group"
                android:title="@string/create_group"/>
    </group>

    <item android:title="@string/account_options">
        <menu>
            <item
                android:id="@+id/logout"
                android:icon="@mipmap/ic_logout"
                android:title="@string/disconnect_account"/>
        </menu>
    </item>

</menu>
